# Google Cloud Platform Configuration
# This file contains GCP project settings for Cloud Build and deployment

# Project Settings
GCP_PROJECT_ID=mlops-group34
GCP_REGION=europe-west1

# Artifact Registry
ARTIFACT_REGISTRY_REPO=docker-repo
ARTIFACT_REGISTRY_URL=europe-west1-docker.pkg.dev/mlops-group34/docker-repo

# Container Image
IMAGE_NAME=forestfires-pipeline
FULL_IMAGE_PATH=europe-west1-docker.pkg.dev/mlops-group34/docker-repo/forestfires-pipeline

# Google Cloud Storage
GCS_BUCKET_NAME=forestfires-data-bucket
GCS_DATA_PATH=gs://forestfires-data-bucket

# Secrets (stored in Google Secret Manager)
# - WANDB_API_KEY: Weights & Biases API key for experiment tracking
